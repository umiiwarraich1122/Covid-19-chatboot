{
  "version": 2,
  "builds": [
    {
      "src": "covid-intel-frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/covid-intel-frontend/$1"
    }
  ]
}
